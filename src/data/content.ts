/**
 * Данные, которые меняются со временем: цены, цифры кейсов, контакты, ссылки.
 *
 * Правило репо №1 - не хардкодить динамику по всей вёрстке. Сайт статичный,
 * значения всё равно печатаются в билд, но живут они в одном месте и с явно
 * указанным источником. Поменялся источник - правим здесь и ПЕРЕСОБИРАЕМ сайт,
 * иначе цифра молча устареет.
 *
 * Источники:
 *   цены   -> business/products/pricing.md   (сверено 2026-07-23)
 *   кейс 1 -> business/life-metrics.md, «Кейс "фитнес-центр": результат бота»
 *             (значения на 2026-07-23, со слов управляющей студией)
 *   тексты -> business/assets/site-copy.md
 */

/** Главный призыв сайта - якорь квиза. Меняется в одном месте.
 *  Больше не единственный: с 2026-08-11 у карточек пакетов есть своя кнопка в
 *  Telegram (план, 4.2 - решение пересмотрено). Главным остаётся квиз, но
 *  держится это размером кнопки, а не цветом: .cta крупнее и со свечением,
 *  .cta-card мельче. Обе фиолетовые - бледную кнопку на карточке не замечают. */
export const QUIZ_ANCHOR = '#quiz';

export type Package = {
  name: string;
  price: string;
  body: string;
  /** вторая строка описания - только там, где она есть в site-copy */
  note?: string;
};

/** Боты и воронки. Порядок принципиален: дорогое сверху, «Кодовое слово» -
 *  последним. Поставить пятитысячный пакет первым значит прочитать весь сайт
 *  как «боты за пять тысяч» (план, раздел 6). */
export const botPackages: Package[] = [
  {
    name: '«Квиз-воронка»',
    price: 'от 15 000 ₽',
    body: 'Тест с баллами: клиент проходит, набирает очки и по сумме получает свой вариант финала. Все финалы ведут к покупке. А вы заодно видите, кто с чем к вам пришёл.',
  },
  {
    name: '«Игровой бот»',
    price: 'от 25 000 ₽',
    body: 'Вовлечение через игру: человек получает баллы за активность, копит их, меняет на коллекционные карточки и идёт за призом. Пока собирает коллекцию - он у вас в блоге каждый день. Призы - ваши же продукты: игра раздаёт то, что вы продаёте, и подводит к покупке.',
    note: 'Игра собирается под ваш бизнес: свои правила, свои карточки, свои призы. Шаблона здесь нет - по договору собранная игра остаётся у заказчика, и я не повторяю её для других. Отсюда и цена, и то, что у ваших конкурентов такого не будет.',
  },
  {
    name: '«Ночной администратор»',
    price: 'от 25 000 ₽',
    body: 'ИИ отвечает на частые вопросы в Instagram и ВКонтакте, предлагает записаться, забирает телефон и передаёт заявку менеджеру. Клиенты не утекают ночью и в выходные.',
  },
  {
    name: '«Магазин в Telegram»',
    price: 'от 15 000 ₽',
    body: 'Витрина товаров прямо в Telegram: клиент листает каталог, кладёт в корзину и оформляет заказ, не выходя из мессенджера.',
    note: 'Дальше управляете сами: добавляете и убираете товары, меняете цены без меня. Магазин остаётся вашим, а не привязанным ко мне.',
  },
  {
    name: '«Кодовое слово»',
    price: '5 000 ₽',
    body: 'Человек пишет слово в Instagram, бот проверяет подписку и выдаёт лид-магнит. Самый простой способ проверить, как я работаю, прежде чем заказывать что-то большое.',
  },
];

/** Сайты и приложения. */
export const siteProducts = [
  { name: 'Лендинг', price: '10 000-25 000 ₽', body: 'Одна страница, которая продаёт.' },
  { name: 'Многостраничный сайт', price: '25 000-50 000 ₽', body: 'Услуги, кейсы, заявки.' },
  { name: 'Приложение', price: 'от 50 000 ₽', body: 'Веб-сервис под вашу задачу.' },
];

/**
 * Строка-факт «Этот сайт собран за N дней. Ваш соберу так же.»
 * N подставляется по факту запуска - это задача подфазы А5, и число должно быть
 * настоящим, а не «примерно». Пока null - строка на странице не выводится:
 * лучше её отсутствие, чем выдуманная цифра.
 */
export const siteBuiltInDays: number | null = null;

/**
 * Клиент Кейса 1. Раскрыт по имени 2026-08-11 решением Лилии: анонимный
 * «фитнес-центр» доверия почти не даёт, живое название и логотип - дают.
 * Разрешение студии на публикацию названия и логотипа получено
 * (`business/assets/testimonials.md`).
 *
 * `logo` - путь к файлу ВНУТРИ `public/`, без ведущего слэша: сайт отдаётся из
 * подпапки `/liliyakozachuk/`, base к пути приклеивает вёрстка. null - файла
 * ещё нет, на странице стоит одно название и вёрстка не ломается (тот же приём,
 * что у telegramUsername и phone ниже: битая картинка хуже её отсутствия).
 *
 * TODO (нужно от Лилии): исходник логотипа студии, png или svg от 300px по
 * длинной стороне, квадратный. Положить в `public/clients/` и вписать сюда путь
 * - логотип включится сам, вёрстку трогать не надо. Скриншот профиля не
 * годится: там 94px и синяя кнопка «+» поверх логотипа.
 */
export const fitnessClient: { name: string; role: string; logo: string | null } = {
  name: 'Happy&Funny',
  role: 'фитнес-студия, две точки',
  logo: null,
};

/** Цифры Кейса 1. Источник - life-metrics.md, не выдумывать. */
export const fitnessCase = {
  before: '2-3',
  after: '4-7',
  unit: 'записи в день',
  lost: '0',
  lostLabel: 'потерянных заявок',
};

/**
 * Цифры Кейса 2. Источник - отзыв Распутиных (`assets/testimonials.md`) и
 * `assets/case-jurist-urapoliya.md`.
 *
 * Переехали сюда 2026-08-11: с появлением тизера под первым экраном они
 * печатаются в двух местах сразу (тизер и экран 5). Две копии одной цифры в
 * вёрстке - ровно то, что запрещает правило репо №1: поправят одну, забудут
 * вторую, и страница сама себе противоречит.
 */
export const juristCase = {
  total: '20+',
  totalLabel: 'ботов запущено',
  live: '5',
  liveLabel: 'работают постоянно и приносят доход',
};

/**
 * Telegram - единственный выход с сайта в Фазе А: и финал квиза, и строка в
 * футере ведут сюда. Форма заявки появится только в Фазе Б, когда будет сервер
 * в РФ (план, 3.5). Username дала Лилия 2026-07-23.
 *
 * Хранится в одном месте: сменится ник - правится одна строка, и меняются оба
 * места сразу. Если поставить null - квиз честно покажет, что кнопка ещё не
 * подключена, а не поведёт человека по битой ссылке.
 */
export const telegramUsername: string | null = 'leto4Kaa';
export const telegramUrl: string | null = telegramUsername
  ? `https://t.me/${telegramUsername}`
  : null;

/**
 * Телефон - для тех, кто не хочет переписку.
 *
 * TODO (нужно от Лилии): вписать номер строкой ровно в том виде, в каком он
 * должен читаться на странице, например '+7 999 123-45-67'. Пока null - строки
 * с телефоном нет ни под витриной, ни в футере: выдуманный контакт хуже
 * отсутствующего (то же правило, что у telegramUsername выше).
 *
 * Ссылка tel: собирается из этой же строки, поэтому второй копии номера в
 * проекте нет и рассинхрона «показываем один, звоним на другой» быть не может.
 *
 * Имейте в виду: номер на открытой странице собирают спам-боты и обзвонщики.
 * Это плата за то, что человек может позвонить, не заводя переписку.
 */
export const phone: string | null = null;
export const phoneHref: string | null = phone
  ? `tel:${phone.replace(/[^\d+]/g, '')}`
  : null;

/**
 * Заготовка сообщения по конкретному пакету.
 *
 * Зачем: Telegram не умеет подставлять текст в личный чат по ссылке (параметр
 * ?text= работает только у ботов и у share-ссылок). Поэтому кнопка на карточке
 * кладёт эту строку в буфер обмена, а человек вставляет её в чат одним жестом.
 * Тот же приём, что на финальном экране квиза со строкой «можно скопировать».
 */
export const orderMessage = (packageName: string) =>
  `Здравствуйте! Пишу с сайта, интересует ${packageName}.`;

/**
 * Контакты футера.
 * TODO (А5, нужны от Лилии): публичная почта, ФИО и ИНН для оферты.
 * Не выдумываем и не подставляем заглушки: неверный контакт хуже отсутствующего.
 * Как появятся - вписать сюда, вёрстка футера подхватит сама.
 */
export const contacts: {
  telegram: string | null;
  phone: string | null;
  phoneHref: string | null;
  email: string | null;
  legalName: string | null;
  inn: string | null;
} = {
  telegram: telegramUrl,
  phone,
  phoneHref,
  email: null,
  legalName: null,
  inn: null,
};

/**
 * Ссылка на свой демо-бот (экран 4).
 * TODO (А0, нужно от Лилии): демо-бот в Salebot ещё не собран. Как появится -
 * вписать сюда ссылку, кнопка на экране «Потрогайте» включится сама.
 */
export const demoBotUrl: string | null = null;
